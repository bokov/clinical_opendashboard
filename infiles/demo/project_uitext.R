txtMainVarStatic <- "The left-most columns represent a large simulated cohort and the others are random sub-cohorts sampled from it."
txtMainVarDynamic <- "The y-axis represents the fraction of a large simulated cohort having at least one occurence of the indicated code. The x-axis represents the same but one of the random sub-cohorts as described by the plot legend. Points above/left of the diagonal line represent codes that are less common in their respective sub-cohorts than in the overall cohort, and points below/right are more common."
txtPageSubtitle <- "Demo"
selBasicDefault <- 'DEM|LANGUAGE'
